  <div class="row">
    <div class="col-md-3">
      <table class="table top-table-outer">
        <thead>
          <tr>
            <th colspan="3", class="top-border-head">ジャンル検索</th>
          </tr>
        </thead>
        <tbody>
          <% genres.each do |genre| %>
            <tr>
              <td colspan="3"><%= link_to genre.name, genre_finding_items_path(genre.id) %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>